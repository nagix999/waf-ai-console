# R3 SOLO Change Log

Compared with R2:

- Sidebar reordered to Overview / Evaluate / Configure / Operate / Connect.
- Promotion removed from Operate tabs and made contextual full-page workflow.
- Overview `Needs Attention` renamed `Action Center`.
- Evaluation trend defaults to 0–100% Y-axis and uses current-evaluation fallback when comparable history is insufficient.
- Ground Truth adds Working Changes, case Revert, Discard All, publish inclusion-rate preview and exclusion acknowledgement.
- Test metric expansion uses disclosure/toggle semantics instead of checkbox semantics.
- R2 stale audit text and obsolete mockup references removed.
